version: '3.8'

services:
  network-bot:
    build: .
    container_name: ping-my-network-bot
    restart: unless-stopped
    env_file:
      - .env
    privileged: true  # Add this - gives full access to host devices
    cap_add:
      - NET_RAW
      - NET_ADMIN
    network_mode: host